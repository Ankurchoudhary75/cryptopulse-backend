services:
  - type: web
    name: cryptopulse-backend
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: PORT
        value: 8080
      - key: SPRING_DATASOURCE_URL
        fromDatabase:
          name: cryptopulse-db
          property: connectionString

databases:
  - name: cryptopulse-db
    databaseName: cryptopulse
    plan: free
